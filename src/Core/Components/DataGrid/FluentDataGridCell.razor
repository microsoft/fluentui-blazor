@namespace Microsoft.FluentUI.AspNetCore.Components
@inherits FluentComponentBase
@typeparam TGridItem

@if (CellType == DataGridCellType.Default || CellType == DataGridCellType.RowHeader)
{
    <td data-col-index=@GridColumn
        class="@ClassValue"
        style="@StyleValue"
        @onkeydown="@HandleOnCellKeyDownAsync"
        @onclick="@HandleOnCellClickAsync"
        @attributes="AdditionalAttributes">
        @ChildContent
    </td>
}
else
{
    <th data-col-index=@GridColumn
        class="@ClassValue"
        style="@StyleValue"
        @onkeydown="@HandleOnCellKeyDownAsync"
        @onclick="@HandleOnCellClickAsync"
        @attributes="AdditionalAttributes">
        @ChildContent
    </th>
}

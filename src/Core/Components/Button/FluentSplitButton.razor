@namespace Microsoft.FluentUI.AspNetCore.Components
@using Microsoft.AspNetCore.Components.Rendering;
@using Microsoft.FluentUI.AspNetCore.Components.Extensions
@inherits FluentComponentBase

<FluentMenu Class="@ClassValue" Style="@StyleValue" OnClick="@OnMenuClick" OnCheckedChanged="@OnMenuCheckedChanged" split="true">
    <FluentButton IconStart="@IconStart"
                  IconOnly="@IconOnly"
                  Appearance="@(Appearance == ButtonAppearance.Default ? null : Appearance)"
                  Shape="@Shape"
                  Size="@Size"
                  BackgroundColor="@BackgroundColor"
                  Color="@Color"
                  OnClick="@OnClick"
                  slot="primary-action">
        @Label
    </FluentButton>
    <FluentMenuButton IconEnd="@IconToggle"
                      IconOnly="@(IconToggle is null ? true : true)"
                      Appearance="@(Appearance == ButtonAppearance.Default ? null : Appearance)"
                      Shape="@Shape"
                      Size="@Size"
                      BackgroundColor="@BackgroundColor"
                      Color="@Color"
                      aria-label="@Title" />
    @ChildContent
</FluentMenu>


@namespace Microsoft.FluentUI.AspNetCore.Components
@using Microsoft.FluentUI.AspNetCore.Components.Extensions
@inherits FluentInputBase<TValue>
@typeparam TValue

@*

ReadOnly="true"
Mode ? https://github.com/microsoft/fluentui/blob/master/packages/web-components/src/slider/slider.spec.ts

*@

@* <CascadingValue Value="this" IsFixed="true">
	ForId="@Id"	 
	AriaLabel="@AriaLabel"
	ChildContent="@LabelTemplate"
*@
	<FluentLabel style="margin-bottom: calc(var(--design-unit) * 3px);">
		@Label
	</FluentLabel>
    <fluent-slider 
	               id="@Id"
				   class="@ClassValue"
			       style="@StyleValue"
			       disabled="@Disabled"
				   min="@FormatValueAsString(Min)"
				   max="@FormatValueAsString(Max)"	
				   value="@FormatValueAsString(Value)"
			       step="@(Step == default ? null : FormatValueAsString(Step))"
			       size="@Size.ToAttributeValue()"
			       orientation="@(Orientation == Orientation.Horizontal ? null : Orientation.ToAttributeValue())"
				   required="@Required"
			       @onchange="@ChangeHandlerAsync">
		@ChildContent
    </fluent-slider>
@* </CascadingValue> *@

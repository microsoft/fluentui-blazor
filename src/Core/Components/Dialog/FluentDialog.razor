@namespace Microsoft.FluentUI.AspNetCore.Components
@inherits FluentComponentBase

<AddTag Name="@(IsDrawer() ? "fluent-drawer" : "fluent-dialog")"
        fuib
        id="@Id"
        class="@ClassValue"
        style="@StyleValue"
        position="@GetAlignmentAttribute()"
        type="@GetModalAttribute()"
        size="@GetSizeAttribute()"
        @onkeydown="@OnKeyDownHandlerAsync"
        @ondialogbeforetoggle="@OnToggleAsync"
        @ondialogtoggle="@OnToggleAsync"
        @attributes="AdditionalAttributes">

    @GetDialogStyle()

    @if (Instance is not null)
    {
        <CascadingValue Value="Instance">
            <DynamicComponent Type="@Instance.ComponentType"
                              Parameters="Instance.Options.Parameters" />
        </CascadingValue>
    }
    else
    {
        @ChildContent
    }

</AddTag>

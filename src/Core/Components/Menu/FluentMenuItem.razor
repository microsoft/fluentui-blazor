@namespace Microsoft.FluentUI.AspNetCore.Components
@using Microsoft.FluentUI.AspNetCore.Components.Extensions
@inherits FluentComponentBase
<fluent-menu-item id="@Id"
                  class="@ClassValue"
                  style="@StyleValue"
                  role="@Role.ToAttributeValue()"
                  checked="@Checked"
                  disabled="@Disabled"
                  @attributes="AdditionalAttributes"
                  @onmenuitemchange="@OnChangeHandlerAsync">
    @if (IconIndicator != null)
    {
        <FluentIcon Value="@IconIndicator" Slot="@(_emptyContent ? null : "indicator")" />
    }
    @if (IconStart != null)
    {
        <FluentIcon Value="@IconStart" Slot="@((_emptyContent) ? null : "start")" />
    }
    @Label
    @ChildContent
    @if (MenuItems != null && MenuList != null)
    {
        <FluentMenuList OnCheckedChanged="@MenuList.OnCheckedChanged" slot="submenu">
            @MenuItems
        </FluentMenuList>
    }
    @if (IconEnd != null)
    {
        <FluentIcon Value="@IconEnd" Slot="@((_emptyContent) ? null : "end")" />
    }
    @if (IconSubmenu != null)
    {
        <FluentIcon Value="@IconSubmenu" Slot="@((_emptyContent) ? null : "submenu-glyph")" />
    }
</fluent-menu-item>

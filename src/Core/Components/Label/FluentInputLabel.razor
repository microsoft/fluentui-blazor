@namespace Microsoft.FluentUI.AspNetCore.Components

@if (!string.IsNullOrEmpty(Label) || ChildContent is not null)
{
    <label for="@ForId" class="fluent-input-label" @attributes="@AdditionalAttributes">
        @Label
        @ChildContent
    </label>
}
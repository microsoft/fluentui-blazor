@namespace Microsoft.FluentUI.AspNetCore.Components
@inherits FluentComponentBase

<FluentIcon Value="@Icon"
            Class="fluent-layout-hamburger"
            display="@(ShowMobileOnly ? "mobile" : "mobile-desktop")"
            Focusable="true"
            OnClick="@HamburgerClickAsync" />

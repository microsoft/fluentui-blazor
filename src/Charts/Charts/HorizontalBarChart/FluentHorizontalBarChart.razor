@namespace Microsoft.FluentUI.AspNetCore.Components.Charts
@using Microsoft.FluentUI.AspNetCore.Components.Extensions
@inherits FluentComponentBase

<fluent-horizontal-bar-chart id="@Id"
                             class="@ClassValue"
                             style="@StyleValue"
                             data="@ChartJson.Serialize(ChartData!)"
                             variant="@(Variant is null ? null : Variant.Value.ToAttributeValue())"
                             hide-ratio="@(HideRatio ? "true" : null)"
                             hide-legends="@(HideLegends ? "true" : null)"
                             hide-tooltip="@(HideTooltip ? "true" : null)"
                             legend-list-label="@LegendListLabel"
                             chart-title="@ChartTitle"
                             @attributes="@AdditionalAttributes">
</fluent-horizontal-bar-chart>

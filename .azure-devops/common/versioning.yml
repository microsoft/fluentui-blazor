variables:
  # File and Package version
  # dev  branch: 1.2.4-preview.1.23282.1'    (PackageSuffix is always ignored in Dev branch)
  # main branch: 1.2.4-rc.1'               (PackageSuffix is ignored, if empty, in Main branch)
  FileVersion: '3.5.1'
  PackageSuffix: ''
@using Xunit
@inherits TestContext

@code
{

    [Fact]
    public void FluentStack_Id()
    {
        // Arrange && Act
        var cut = Render(@<FluentStack Id="customid">My content</FluentStack>);

        //Assert
        cut.Verify();
    }
}
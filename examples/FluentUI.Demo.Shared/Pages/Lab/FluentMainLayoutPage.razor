@page "/Lab/FluentMainLayout"

<h2>MainLayout</h2>
<ApiDocumentation Component="typeof(FluentMainLayout)" />

<DemoSection Component="typeof(FluentMainLayout)" Title="FluentMainLayout" CollocatedFiles="@(new[] {"cs"})" ComponentParameters="@MainLayoutParameters">

</DemoSection>

@code {
    Dictionary<string, object> MainLayoutParameters = new()
    {
        {"Header", (RenderFragment)(builder => builder.AddMarkupContent(0,"<h3>Fluent UI Components</h3>"))},
        {"SubHeader", (RenderFragment)(builder => builder.AddMarkupContent(1,"<p>A subheader here</p>"))},
        {"Body", (RenderFragment)(builder => builder.AddMarkupContent(2,"<p>Lorum ipsum</p>"))},
        {"NavMenuItems", (RenderFragment)(builder => {
                builder.OpenComponent<FluentNavLink>(3);
                builder.AddAttribute(4, "Href", "/");
                builder.AddAttribute(5, "Text", "Home");
                builder.CloseComponent();
            }
        )},
        {"NavMenuTitle", "Navigation menu"},
    };
}

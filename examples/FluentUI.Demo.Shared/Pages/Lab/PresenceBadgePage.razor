@page "/Lab/PresenceBadge"
@* Remember to replace the namespace below with your own project's namespace. *@
@namespace FluentUI.Demo.Shared

<h1>PresenceBadge</h1>

<p>
    The <code>PresenceBadge</code> component is used to display a status indicator such as available, away, or busy.
</p>

<ApiDocumentation Component="typeof(PresenceBadge)" />

<DemoSection Component="typeof(PresenceBadge)" Title="PresenceBadge" 
             CollocatedFiles="@(new[] {"cs", "css"})" ComponentParameters="@PresenceBadgeParameters">

</DemoSection>

<h2>Examples</h2>
<DemoSection Component="typeof(PresenceBadgeDefault)" Title="Default">
    <Description>
        <p>
            A presence badge supports <code>Available</code>, <code>Away</code>, <code>Busy</code>, <code>DoNotDisturb</code>,
            <code>Offline</code>, <code>OutOfOffice</code> and <code>Unknown</code> status. The default status is <code>Available</code>.
        </p>
    </Description>
</DemoSection>

<DemoSection Component="typeof(PresenceBadgeOOO)" Title="Out Of Office">
    <Description>
        <p>
            A presence badge supports <code>Available</code>, <code>Away</code>, <code>Busy</code>, <code>DoNotDisturb</code>, 
            <code>Offline</code>, <code>OutOfOffice</code> and <code>Unknown</code> status when <code>OutOfOffice</code> is set to <code>true</code>.
        </p>
    </Description>
</DemoSection>

@code{
    Dictionary<string, object> PresenceBadgeParameters = new()
    {
        {"ChildContent", (RenderFragment)(builder => {
                builder.OpenComponent<FluentIcon>(0);
                builder.AddAttribute(1, "Name", FluentIcons.Person);
                builder.AddAttribute(2, "Size", IconSize.Size32);
                builder.CloseComponent();
            }
        )}
    };
}
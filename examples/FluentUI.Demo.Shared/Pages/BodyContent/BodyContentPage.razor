@page "/BodyContent"

<h1>BodyContent</h1>
<p>
    The <code>FluentBodyContent</code> component is a placeholder for where the actual site content needs to be shown. It comes with some sensible CSS default settings defined.
</p>

<ApiDocumentation Component="typeof(FluentBodyContent)" />

<h2>Example</h2>
<DemoSection Component="typeof(FluentBodyContent)" Title="BodyContent" HideAllButExample=true ComponentParameters="@(new Dictionary<string, object>(){ {"ChildContent",(RenderFragment)(builder => builder.AddMarkupContent(1,"This is some content displayed in a FluentBodyContent component.")) } })">
    <ChildContent>
      <CodeSnippet>
&lt;FluentBodyContent&gt;
    This is some content displayed in a FluentBodyContent component.
&lt;/FluentBodyContent&gt;
      </CodeSnippet>
    <p>Renders as:</p>
    </ChildContent>
</DemoSection>

@code {

}

@page "/Footer"

<h1>Footer</h1>

<p>
    The <code>Footer</code> component is used display a colored bar (with text and possibly other components) at the bottom of a site or application. It can be styled with regular CSS.
</p>

<ApiDocumentation Component="typeof(Footer)" />

<h2>Example</h2>
<DemoSection Component="typeof(Footer)" 
             Title="Footer" 
             CollocatedFiles="@(new[] {"cs", "css"})" 
             ComponentParameters="@(new Dictionary<string, object>(){ {"ChildContent",(RenderFragment)(builder => builder.AddMarkupContent(1,"Footer text")) } })">
        <CodeSnippet>
&lt;Footer&gt;
    Footer text
&lt;/Footer&gt;
        </CodeSnippet>
        <p>Renders as:</p>
</DemoSection>
@code {

}

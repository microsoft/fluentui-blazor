@if (Properties == null)
{
    <FluentProgressRing />
}
else
{
    <FluentTreeView>
        @foreach (var property in Properties)
        {
            <ApiComplexClassItem Property="@property" />
        }
    </FluentTreeView>
}
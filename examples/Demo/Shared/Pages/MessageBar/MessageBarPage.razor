@page "/MessageBar"
@using FluentUI.Demo.Shared.Pages.MessageBar.Examples

<h2>Examples</h2>

<DemoSection Title="Simple messages" Component="typeof(MessageBarSimple)">
    <p>This example show the different intents that can be shown in a MessageBar.</p>
</DemoSection>

<DemoSection Title="Simple notification" Component="typeof(MessageBarSimpleNotification)">
    <p>This example show how a MessageBar can be used to display a notification.</p>
</DemoSection>

<DemoSection Title="Message Service" Component="typeof(MessageBarDefault)">
</DemoSection>

<DemoSection Title="MessageBar with options" Component="typeof(MessageBarDetailed)">
</DemoSection>

<h3>Notification Center</h3>

<DemoSection Title="Sample button (top/right) to display the number of alerts"
             Component="typeof(SimpleNotificationButton)">
    
</DemoSection>

<DemoSection Title="Notification center" Component="typeof(SimpleNotificationCenterDialog)" />

<h2>Service injection</h2>

<h4>Injection with global options.</h4>
<CodeSnippet>
    @(@"builder.Services
       .AddScoped&lt;IMessageService, MessageService>()")
</CodeSnippet>

<h4>Injection with global options.</h4>
<CodeSnippet>
    @(@"builder.Services
       .AddScoped<IMessageService>(builder => new MessageService(builder.GetRequiredService<NavigationManager>(), new MessageBarGlobalOptions()
       {
           ShowDismiss = true,
           ClearAfterNavigation = false,
           MaxMessageCount= 5,
           NewestOnTop = true,
       }));")
</CodeSnippet>

<h4>Display the list of messages</h4>
You can display the list of messages using the component <b>FluentMessageBarContainer</b>.
<CodeSnippet>
    @(@"<FluentMessageBarContainer Category=""MESSAGE_CATEGORY"" Format=""MessageFormat.Notification"" />")
</CodeSnippet>

<h2>API Documentation</h2>
<ApiDocumentation Component="typeof(FluentMessageBarContainer)" />

<ApiDocumentation Component="typeof(FluentMessageBar)" />

<ApiDocumentation Component="typeof(MessageBarService)" />

<ApiDocumentation Component="typeof(MessageBarContent)" />
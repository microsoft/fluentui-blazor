@page "/WhatsNew-Archive"

<PageTitle>@App.PageTitle("What's new? (archive)")</PageTitle>

<h1>What's new?</h1>

<p>Here you can find the release notes for the FluentUI components library before version 4.</p>

<MarkdownSection FromAsset="./_content/FluentUI.Demo.Shared/docs/WhatsNew-Archive.md" OnContentConverted="RefreshTableOfContents" />

@code {

    [CascadingParameter]
    public EventCallback OnRefreshTableOfContents { get; set; }

    private async Task RefreshTableOfContents()
    {
        await OnRefreshTableOfContents.InvokeAsync();
    }
}

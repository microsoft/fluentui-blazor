@page "/Charts/SankeyChart/Debug"
@using Microsoft.FluentUI.AspNetCore.Components.Charts

<div style="margin:20px;display:flex;flex-wrap:wrap;gap:16px 24px;align-items:end;">
    <FluentField LabelPosition="LabelPosition.Above"
                 Style="min-width:220px;flex:1 1 220px;">
        <label slot="label" for="basic-width" id="basic-width--label">
            Width
        </label>
        <FluentSlider slot="input"
                      id="basic-width"
                      min="200"
                      max="1000"
                      @bind-Value="@width">
        </FluentSlider>
        <fluent-label slot="message">@width</fluent-label>
    </FluentField>
    <FluentField LabelPosition="LabelPosition.Above"
                 Style="min-width:220px;flex:1 1 220px;">
        <label slot="label" for="basic-height" id="basic-height--label">
            Height
        </label>
        <FluentSlider slot="input"
                      id="basic-height"
                      min="200"
                      max="1000"
                      @bind-Value="@height">
        </FluentSlider>
        <fluent-label slot="message">@height</fluent-label>
    </FluentField>
    <FluentSwitch Label="Hide labels" @bind-Value="hideLabels"></FluentSwitch>
    <FluentSwitch Label="Hide legends" @bind-Value="hideLegends"></FluentSwitch>
    <FluentSwitch Label="Hide Tooltips" @bind-Value="hideTooltips"></FluentSwitch>
</div>

<div style="margin:20px;">
    <FluentSankeyChart ChartTitle="Sankey chart interactive example"
                       ChartData="@chartData"
                       PathColor="#0078D4"
                       Width="@($"{width}px")"
                       Height="@($"{height}px")"
                       HideLabels="@hideLabels"
                       HideTooltip="@hideTooltips"
                       HideLegends="@hideLegends" />
</div>

@code {
    private readonly SankeyChartData chartData = new()
    {
        Nodes =
        [
            new SankeyChartNode { Name = "Source A", Color = DataVizPalette.Color1 },
            new SankeyChartNode { Name = "Source B", Color = DataVizPalette.Color2 },
            new SankeyChartNode { Name = "Target C", Color = DataVizPalette.Color3 },
            new SankeyChartNode { Name = "Target D", Color = DataVizPalette.Color4 },
        ],
        Links =
        [
            new SankeyChartLink { Source = 0, Target = 2, Value = 50 },
            new SankeyChartLink { Source = 1, Target = 2, Value = 30 },
            new SankeyChartLink { Source = 1, Target = 3, Value = 20 },
        ],
    };

    private int width = 650;
    private int height = 350;
    private bool hideLabels = true;
    private bool hideLegends;
    private bool hideTooltips;
}

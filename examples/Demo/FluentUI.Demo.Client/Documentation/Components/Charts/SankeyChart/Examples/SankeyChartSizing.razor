@using Microsoft.FluentUI.AspNetCore.Components.Charts

<FluentStack Orientation="Orientation.Horizontal" HorizontalGap="4">
    <FluentSlider Label=@($"Width: {_width}") @bind-Value="_width" Id="sankey-width" Min="200" Max="1000" Step="25" Style="width:300px;" />
    <FluentSlider Label=@($"Height: {_height}") @bind-Value="_height" Id="sankey-height" Min="200" Max="1000" Step="25" Style="width:300px;" />
</FluentStack>

<FluentSankeyChart ChartTitle="Sankey chart sizing example"
                   ChartData="@data"
                   Width="@($"{_width}px")"
                   Height="@($"{_height}px")" />

@code {
    private int _width = 820;
    private int _height = 412;

    private readonly SankeyChartData data = new()
    {
        Nodes =
        [
            new SankeyChartNode { Name = "node0", Color = DataVizPalette.Custom, CustomColor = "qualitative.2" },
            new SankeyChartNode { Name = "node1", Color = DataVizPalette.Custom, CustomColor = "qualitative.7" },
            new SankeyChartNode { Name = "node2", Color = DataVizPalette.Custom, CustomColor = "qualitative.8" },
        ],
        Links =
        [
            new SankeyChartLink { Source = 0, Target = 2, Value = 2 },
            new SankeyChartLink { Source = 1, Target = 2, Value = 2 },
        ],
    };
}

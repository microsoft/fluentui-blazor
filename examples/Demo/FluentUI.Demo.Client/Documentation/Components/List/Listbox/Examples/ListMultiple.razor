<div>
    Selected items: @(string.Join(", ", SelectedItems))
</div>

<FluentListbox Label="Color"
               Items="@Colors"
               TOption="string"
               TValue="string"
               @bind-SelectedItems="@SelectedItems"
               Multiple="true" />

@code {
    static string[] Colors = ["Red", "Green", "Blue"];
    IEnumerable<string> SelectedItems = new[] { Colors[1] };
}
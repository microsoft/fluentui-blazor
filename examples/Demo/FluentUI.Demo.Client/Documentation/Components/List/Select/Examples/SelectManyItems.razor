<FluentSelect Label="500 Names"
              Placeholder="Select a name"
              Items="@Persons"
              Multiple="true"
              OptionText="@(i => $"[{i?.Id:0000}] {i?.FirstName} {i?.LastName}")"
              OptionValue="@(i => $"{i?.Id}")"
              Height="200px"
              Width="300px"
              @bind-Value="@Selected" />

<div>
    Selected: @Selected?.FirstName
</div>

@code {
    static SampleData.People.Person[] Persons = SampleData.People.GeneratePersons(500, sequentialId: true).ToArray();
    SampleData.People.Person? Selected;
}
